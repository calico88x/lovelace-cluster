apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: radio-public
  namespace: radio
spec:
  secretName: radio-public-tls

  dnsNames:
    - radio.caliconet.ca

  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
    group: cert-manager.io
