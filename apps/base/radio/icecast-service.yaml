apiVersion: v1
kind: Service
metadata:
  name: icecast
  namespace: radio
  labels:
    app: icecast
spec:
  type: ClusterIP
  selector:
    app: icecast
  ports:
    - name: http
      port: 8000
      targetPort: http

    - name: metrics
      port: 9119
      targetPort: metrics
