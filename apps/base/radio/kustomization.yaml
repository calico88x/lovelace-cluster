apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: radio

resources:
  - namespace.yaml
  - storage.yaml
  - icecast-deployment.yaml
  - icecast-service.yaml
  - liquidsoap-configmap.yaml
  - liquidsoap-deployment.yaml
  - liquidsoap-service.yaml

configMapGenerator:
  - name: icecast-exporter
    files:
      - icecast-exporter.py
